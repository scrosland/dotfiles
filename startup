# vim: set filetype=sh: 
# 
# Simply source all the individual scripts
#
for script in environment functions shrc ; do
    . "$HOME/dotfiles/${script}"
    lcl="$HOME/.${script}.local"
    if [ -r "${lcl}" ] ; then
        . "${lcl}"
    fi
done
